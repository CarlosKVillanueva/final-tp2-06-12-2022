export default class Notificador {
    notificadorMail({magnitud, unidadTemperatura }) {
        console.log(`Registramos una tempertura record!: (magnitud: ${magnitud}, Unidad de Temperatura: ${unidadTemperatura})`)
    }
}